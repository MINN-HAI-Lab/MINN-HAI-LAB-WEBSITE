{
  "name": "minn-hai-lab",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "e2e": "playwright test",
    "verify": "npm run check && npm run build && astro preview --port 4331 && npm run e2e; s=$?; astro preview stop >/dev/null 2>&1; exit $s"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.63.0",
    "astro": "^7.3.2",
    "typescript": "^6.0.3"
  }
}
