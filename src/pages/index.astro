---
import Base from '../layouts/Base.astro';
import Trace from '../components/Trace.astro';
---

<Base title="MINN HAI Lab">
  <section class="section">
    <h1 class="measure">MINN HAI Lab</h1>

    <p class="todo measure">
      TODO: confirm the lab name as it should appear in the header. The brief
      uses "MINN HAI Lab", "MINN HAI (Human-centered AI) Lab" and "HAI Lab"
      interchangeably, and whether it is "Human-centered" or "Human-centred"
      is undecided. Kaung to confirm. Tracked as B-1.
    </p>

    <p class="todo measure">
      TODO: the mission sentence. The plan lists the mission paragraph as
      already supplied, but the text is not in the repo, so there is nothing to
      cut to one line. Kaung to supply. Tracked as B-3.
    </p>
  </section>

  <section class="section">
    <Trace />
  </section>
</Base>
